# ProtonVPN Connection Manager

A lightweight Python wrapper built on top of [`protonvpn_nm_lib`](https://github.com/ProtonVPN/linux-cli-community) that provides simple methods to **login**, **logout**, **connect**, **reconnect**, and **disconnect** from ProtonVPN servers.

Project URL: [View on GitHub]({url})

---

## Features

- Login & Logout support  
- Connect 
- Reconnect
- Disconnect
- Built on top of `protonvpn_nm_lib` for reliability and compatibility  

---

## Installation

```bash
git clone {url}
cd protonvpn-connection-manager
pip install -r requirements.txt
